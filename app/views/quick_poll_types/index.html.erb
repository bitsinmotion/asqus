<h1>Listing quick_poll_types</h1>

<table>
  <tr>
    <th>Type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @quick_poll_types.each do |quick_poll_type| %>
  <tr>
    <td><%= quick_poll_type.type %></td>
    <td><%= link_to 'Show', quick_poll_type %></td>
    <td><%= link_to 'Edit', edit_quick_poll_type_path(quick_poll_type) %></td>
    <td><%= link_to 'Destroy', quick_poll_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Quick poll type', new_quick_poll_type_path %>
