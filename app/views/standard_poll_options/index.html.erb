<h1>Listing standard_poll_options</h1>

<table>
  <tr>
    <th>Standard poll option set</th>
    <th>Text</th>
    <th>Value</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @standard_poll_options.each do |standard_poll_option| %>
  <tr>
    <td><%= standard_poll_option.standard_poll_option_set_id %></td>
    <td><%= standard_poll_option.text %></td>
    <td><%= standard_poll_option.value %></td>
    <td><%= link_to 'Show', standard_poll_option %></td>
    <td><%= link_to 'Edit', edit_standard_poll_option_path(standard_poll_option) %></td>
    <td><%= link_to 'Destroy', standard_poll_option, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Standard poll option', new_standard_poll_option_path %>
